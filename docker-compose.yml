version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: tapmiweb
    ports:
      - 3013:80 
 # app:
 #   build:
 #     context: ./
 #     dockerfile: app.dockerfile
 #   volumes:
 #    - ./:/var/www


