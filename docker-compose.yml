version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: tapmiweb
    ports:
      - 8181:80 
 # app:
 #   build:
 #     context: ./
 #     dockerfile: app.dockerfile
 #   volumes:
 #    - ./:/var/www


